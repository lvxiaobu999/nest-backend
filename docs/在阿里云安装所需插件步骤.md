```bash
# 安装git
sudo apt update && sudo apt install git -y

# 安装nvm
sudo sh -c 'echo ". ~/.nvm/nvm.sh" >> /etc/profile && echo "export NVM_NODEJS_ORG_MIRROR=https://npmmirror.com/mirrors/node" >> /etc/profile'

# 安装node
nvm install 24
nvm install 24.15.0 # 注意具体看自己安装了哪个版本

# 安装pnpm
npm i -g pnpm
```
